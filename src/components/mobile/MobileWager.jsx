import React, { useContext, useEffect } from 'react';

import { SocketContext } from '../../context/socket';

const MobileWager = (props) => {
    const socket = useContext(SocketContext);

    useEffect(() => {
    });

    return (
        <div>
        </div>
    );
};

export default MobileWager;
