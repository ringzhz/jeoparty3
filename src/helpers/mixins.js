import { css } from 'styled-components';

const flexAlignCenter = css`
    display: flex;
    justify-content: center;
    align-content: center;
    flex-direction: column;
`;

const mixins = { flexAlignCenter };
export default mixins;
